#include "utils.h"


void testFunc() {
	//save the X position in a local float of where the text first start
	//each sentance is a vector of words
	//loop to add each next word checks the size of the current master string
	// add \n to master string if the next word wont fit


}

